fix: resolve frontend path issues and add missing scan data fields

- Add apps/__init__.py and apps/backend/app.py CORS setup so the backend
  runs correctly and the frontend can reach it
- Fix broken relative paths in index.html, dashboard.html, report.html,
  app.js, dashboard.js, report.js (frontend files are flat in
  apps/frontend/, not in nested subfolders)
- Add target, started_at, completed_at, and events fields to the scan
  status API response
- Add add_scan_event() helper to scan_store.py and wire it into the
  orchestrator progress callback so the live log terminal, event
  counter, and worker counter now populate correctly
- Verified end-to-end: full scan flow, live dashboard updates, PDF/JSON
  downloads, all working with zero console errors
- 121/121 tests passing
