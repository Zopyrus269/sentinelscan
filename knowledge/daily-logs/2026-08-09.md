---
type: knowledge-vault-daily-log
date: 2026-08-09
updated_by: claude-code
---

# 2026-08-09

## What happened

Two work passes today, both user-verified and committed.

### Pass 1 — sync `main` and get to a running local app

- Local `main` had diverged from `origin/main`: local was 8 commits ahead (all `knowledge/`/`.claude/`/MCP tooling from 2026-08-07, no `apps/`/`tests/` overlap), `origin/main` was 4 commits ahead with real product work (orchestrator rewrite, worker fixes, frontend JS/CSS updates, new tests). Merged cleanly with `git merge origin/main` — no conflicts (merge commit `a31bf0d`).
- `requirements.txt` gained `pytest-playwright` and friends upstream; installed into the existing `.venv`.
- Full suite: 133 passed, 1 skipped (up from the pre-merge 133/1 — same counts, upstream's new tests came with the merge). README's "123 tests" claim is stale either way (see `CLAUDE.md` §9); not fixed, just re-confirmed still inaccurate.
- Started the Flask app (`python -m apps.backend.app`, port 5000) for the user to manually test.

### Pass 2 — 3 bugs from manual testing

1. **Dark-mode "white border" boxes on the home page.** Root cause: `.glass-card` in `apps/frontend/static/css/styles.css` had a hardcoded light-theme border (`#e2e8f0`) with no dark-mode override, unlike its background which was already theme-aware. This was frontend/design work, delegated per the user's standing rule: I wrote a prompt (using the newly-installed `.agents/skills/ui-ux-pro-max/` skill for guidance), the user ran it through Gemini 3.1 Pro, I reviewed and integrated the diff. Fix: `.glass-card` border now uses the existing `--ss-border` CSS variable instead of a hardcoded hex. Gemini also found and fixed related dark-mode contrast gaps (scrollbar thumb colors, `.text-error`/`.text-warning` utilities) while following the skill's dark-mode workflow.
2. **Gemini orchestration 401 UNAUTHENTICATED.** Reproduced directly against the raw `google.genai` SDK with the local `.env` key — confirmed it was an invalid/wrong credential value, not a bug in `gemini_client.py`. **Important correction made mid-fix:** I initially added a guardrail to `GeminiClient.__init__` rejecting any key not starting with `AIza`, assuming that prefix is required. This was wrong — Google issues valid Gemini Developer API keys in more than one format (the user's replacement key, which works, starts with `AQ.`), so the guardrail would have rejected a real, working key. Removed it (`196ae21` supersedes the guardrail added in `db18751`). **Second issue found on retest:** after the user pasted a new key into `.env`, the scan still failed with the same 401 — because the already-running Flask process had `load_dotenv()`'d the old key at startup and never re-read `.env` (editing `.env` does not hot-reload; Werkzeug's debug reloader only watches `.py` files). Killed the stale process, started a fresh one, verified a full live scan of `example.com` end-to-end (all 10 evidence workers + CVSS + PDF/JSON report generation) with no auth errors.
3. **Dead dashboard footer links.** All 5 footer links in `apps/frontend/dashboard.html` (Documentation, API Reference, Privacy Policy, Terms of Service, Responsible Disclosure) were literal `href="#"` placeholders — the only place in the frontend with this problem. Documentation/Privacy/Terms already had real pages, so those were pure `href` fixes (done by Gemini as part of the same prompt as issue 1... actually as part of the dedicated footer-links prompt). API Reference and Responsible Disclosure had no page or route at all: I added the two Flask routes (`/api-reference`, `/responsible-disclosure`) in `apps/backend/app.py` directly (backend, not delegated), wrote the page copy (API Reference sourced from `docs/API.md` plus the auth/history endpoints that doc doesn't cover; Responsible Disclosure as a standard passive-recon-only + safe-harbor policy with a placeholder contact address), and handed Gemini a prompt to build the two new HTML pages matching the existing site style. Reviewed and fixed one factual inaccuracy Gemini introduced (claimed `/api/v1/auth/session` "sets a secure cookie" — it doesn't, it's a stateless Bearer-token endpoint) before integrating.

### Pass 3 — team secrets bootstrap system

Built and fully verified end-to-end the feature deferred from Pass 2: teammates can now fetch the shared dev secrets from Firestore instead of manually re-typing them, gated to allowlisted developers, without a new/second login method on the site.

- **Design choice (user's call):** reuse the site's existing Google Sign-In (Firebase Auth) rather than a shared password — each teammate authenticates as themselves.
- **New backend:** `require_developer` decorator in `apps/backend/auth/auth_utils.py` (layered on the existing `require_auth`, checks a `developers/{uid}` Firestore doc), new `GET /api/v1/dev/bootstrap-secrets` route in `apps/backend/routes/dev_routes.py` (registered in `app.py`), returns the `config/secrets` Firestore doc to allowlisted callers only.
- **New scripts** (`scripts/`, dev tooling, not part of the deployed app): `admin_seed_secrets.py` (copies local `.env` → `config/secrets`), `admin_add_developer.py --email ...` (allowlists a teammate by looking up their existing Firebase user record), `bootstrap_env.py` (teammate-facing: installed-app Google OAuth flow → exchange for a Firebase ID token via `accounts:signInWithIdp` → call the new endpoint → merge results into local `.env`). Plus `scripts/requirements.txt` (`google-auth-oauthlib`, kept out of the main `requirements.txt` since it's dev-only) and `scripts/README.md`.
- **`scripts/oauth_client.json`** (the Desktop OAuth client config) is committed — per Google's own guidance, installed-app client secrets aren't confidential, same category as the Firebase Web API key already public in `auth.js`.
- **Bug hit and fixed during setup, not in code:** the user's first OAuth client was created under the wrong GCP project (`sentinelscan-3f82d-504922`, a different project number) instead of the actual Firebase project (`sentinelscan-3f82d`, project number `60214574079`). Firebase's `signInWithIdp` rejected the token exchange with `INVALID_IDP_RESPONSE` because the token's audience wasn't authorized for that project. Fixed by having the user explicitly select the correct project in the Cloud Console project switcher before recreating the OAuth client — worth remembering if this ever needs to be redone (e.g. for a production OAuth client during deployment).
- **Verified fully end-to-end, interactively with the user:** `admin_seed_secrets.py` → `admin_add_developer.py --email shreyasrangadi532@gmail.com` → `bootstrap_env.py` (real browser Google sign-in) → confirmed `.env` was written cleanly (no duplicate keys, no corruption) with the correct values round-tripped through Firestore.
- **New standing constraint surfaced by this feature (now in `NEXT_TASK.md`):** always run the app locally as `http://localhost:5000` — Firebase's authorized-domains list only covers `localhost`, and both the site's normal Google Sign-In and this bootstrap system's OAuth exchange depend on that origin.

## Files changed (Pass 3)

- `apps/backend/auth/auth_utils.py` — added `require_developer`.
- `apps/backend/routes/dev_routes.py` — new blueprint.
- `apps/backend/app.py` — registered `dev_bp`.
- `tests/test_dev_routes.py` — new (401/403/404/200 cases, mocked Firebase + Firestore).
- `scripts/admin_seed_secrets.py`, `scripts/admin_add_developer.py`, `scripts/bootstrap_env.py`, `scripts/requirements.txt`, `scripts/README.md`, `scripts/oauth_client.json` — new.

Commits: `8e44d49` (backend + scripts implementation), `0c02bdb` (working OAuth client config, added after live verification).

## Files changed (Pass 1 & 2, for completeness)

- `apps/backend/agent/gemini_client.py` — (net) no functional change vs. before today; a key-format guardrail was added then removed in the same day, see correction above.
- `apps/backend/app.py` — added `/api-reference` and `/responsible-disclosure` routes.
- `apps/frontend/dashboard.html` — fixed all 5 footer `href`s.
- `apps/frontend/static/css/styles.css` — `.glass-card` border theme-fix, scrollbar/text-error/text-warning dark-mode contrast fixes.
- `apps/frontend/api-reference.html`, `apps/frontend/responsible-disclosure.html` — new pages.
- `tests/test_gemini_client_offline.py` — touched then reverted alongside the guardrail add/remove; net no change.

Commits: `a31bf0d` (merge), `db18751` (superseded guardrail + routes), `196ae21` (corrected fix + integrated frontend work).

## Decisions worth remembering

- **Don't assume a Gemini API key format.** Google issues valid keys in more than one prefix format; do not add validation that rejects a key based on prefix pattern-matching. If a key looks wrong, the only reliable check is a live API call, not string shape.
- **Restart the Flask dev server after any `.env` change.** `load_dotenv()` only runs at process startup; there's no hot-reload for env files (only `.py` changes trigger Werkzeug's debug reloader).
- **Always create Google Cloud OAuth clients for this project inside `sentinelscan-3f82d` specifically** — verify the project selector before creating credentials, since GCP will silently let you create one in an unrelated/auto-generated project and Firebase will reject the resulting tokens with `INVALID_IDP_RESPONSE`.
- **Always run the local dev server as `http://localhost:5000`** — Firebase's authorized-domains whitelist for this project only covers `localhost`; a different host/port breaks Google Sign-In popups (both the site's own login and the bootstrap system's OAuth exchange) unless that origin is deliberately added to Firebase first.

## Testing performed

- `pytest tests/` — 133 passed/1 skipped after Pass 1 & 2; 137 passed/1 skipped after Pass 3 (added `test_dev_routes.py`).
- Manual (Pass 2): user tested dark mode, all 5 footer links, and a live scan in the browser — confirmed working.
- Manual (Pass 2, additional): I drove a live scan directly against the running server (`POST /api/v1/scans` → polled to `COMPLETED`, 100%, report generated) to verify the 401 fix independent of the user's own test.
- Manual (Pass 3): full interactive walkthrough with the user — seed, allowlist, and bootstrap-fetch all run for real against the live Firestore project, including recovering from the wrong-GCP-project OAuth error.

## Current project state

App runs cleanly end-to-end locally on `localhost:5000` (single Flask process serves both API and static frontend). All bugs from Pass 2 and the full team-secrets-bootstrap system from Pass 3 are implemented, verified, and committed. Working tree is clean except pre-existing untracked hygiene items (empty `LICENSE.md`, `node_modules/`) and the `.agents/skills/ui-ux-pro-max/` skill folder the user installed locally (still not yet decided whether to commit it — unchanged from Pass 2, still flagged not actioned).

## Next steps

See `knowledge/NEXT_TASK.md` (overwritten alongside this entry). Deployment (free auto-deploy host) is next up.
