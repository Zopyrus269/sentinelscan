---
date: 2026-08-07
commits: [6aef0ca, 386284a, 2d1e4bd, 97590ea]
---

## Set up persistent Claude Code development workflow

**What & why:** The user wants Claude Code to act as a long-term technical partner for SentinelScan: analyze-then-plan-then-approve for new features, delegate frontend implementation to Gemini via a self-contained handoff plan, use sub-agents only when they earn their cost, and maintain a persistent project-memory vault (`knowledge/`) that any fresh session (or Gemini, for frontend work) can read cold with zero prior conversational context.

**Files changed (across 4 commits):**
- `6aef0ca` — `.gitignore`: added `.claude/settings.local.json` and `.obsidian/` (local-only, not project content).
- `386284a` — `knowledge/PROJECT_CONTEXT.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `NEXT_TASK.md`, `daily-logs/2026-08-07.md`, `frontend-plans/.gitkeep`.
- `2d1e4bd` — `CLAUDE.md` (new, repo root), `docs/AGENTS.md` (one-line supersede pointer added).
- `97590ea` — `.mcp.json` (filesystem MCP scoped to `./knowledge`), `.claude/settings.json` (MCP tool allow-list + PostToolUse commit hook), `.claude/hooks/post_commit_vault_reminder.py`.

**Architecture changes:** None to `apps/`. This is process/tooling only — see `knowledge/ARCHITECTURE.md`.

**New components:** `CLAUDE.md` workflow entry point; `knowledge/` vault; `knowledge-vault` filesystem MCP server; a `PostToolUse` hook on `Bash`/`git commit` that injects a vault-update reminder into context (verified live — see Testing below).

**Decisions made:** See `knowledge/DECISIONS.md` — vault stays plain-markdown/Obsidian-independent; `docs/AGENTS.md` kept with a supersede pointer rather than deleted or merged; post-commit hook is a soft nudge not a hard gate; no custom sub-agents defined yet.

**Bugs fixed:** None — this was a greenfield setup, not a bug-fix session.

**Testing performed:**
- Validated `.claude/settings.json` and `.mcp.json` as well-formed JSON (`python -m json.tool`).
- Pipe-tested `post_commit_vault_reminder.py` directly with synthetic `git commit` and non-commit stdin payloads — fires only on the former.
- Confirmed live: the hook actually fired after each of the 3 real commits following its creation, surfacing the expected reminder text each time.
- Ran `npx -y @modelcontextprotocol/server-filesystem ./knowledge` manually — confirmed the relative path arg resolves against the project root (matches the plan's assumption, no absolute-path workaround needed) and the server starts correctly ("Secure MCP Filesystem Server running on stdio").
- **Not yet done:** live in-session MCP connection check via Claude Code's own MCP tools — a newly added `.mcp.json` server isn't picked up by an already-running session. Needs a Claude Code restart/reconnect, then verify `knowledge-vault` shows connected and that reads outside `knowledge/` are refused.

**Current project state:** See `knowledge/PROJECT_CONTEXT.md`. The AI-workflow setup itself is functionally complete and committed; the one open item is the post-restart MCP live-connection check.

**Recommended next steps:** After restarting/reconnecting Claude Code, verify the `knowledge-vault` MCP connection (list `knowledge/` contents through the tool; confirm an out-of-vault read is refused). After that, this workflow is ready for real feature requests.
